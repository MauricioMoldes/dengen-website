const config = {
  GENOME_AGGREGATION_BROWSER: process.env.REACT_APP_GENOME_AGGREGATION_BROWSER,
  ALLELE_FREQUENCY_BROWSER: process.env.REACT_APP_ALLELE_FREQUENCY_BROWSER,
  // Add more here if needed
};

export default config;